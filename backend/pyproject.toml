[project]
name = "metriguard-backend"
version = "1.0.0"
description = "Backend API for packaged commodity compliance inspection"
requires-python = ">=3.11"

dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "alembic",
    "pydantic",
    "python-multipart",
    "opencv-python",
    "paddleocr",
    "pillow",
    "reportlab>=5.0.0"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "httpx",
    "ruff",
    "mypy",
    "pypdf>=5.0.0"
]

[tool.pytest.ini_options]
testpaths = ["tests"]